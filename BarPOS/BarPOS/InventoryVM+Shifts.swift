import Foundation

extension InventoryVM {
    // Intentionally empty.
    // Shift logic now lives in InventoryVM.swift (beginShift / settleShift / running totals).
    // Add non-conflicting helpers here later if needed.
}
